import { Component, signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { Router } from '@angular/router';import { AuthService } from '../../core/auth/auth.service';
@Component({selector:'app-change-password',standalone:true,imports:[FormsModule],templateUrl:'./change-password.component.html',styleUrl:'../login/login.component.css'})
export class ChangePasswordComponent{actual='';nueva='';confirmar='';error=signal('');loading=signal(false);constructor(private auth:AuthService,private router:Router){}async guardar(){if(!this.actual||this.nueva.length<8){this.error.set('La nueva contraseña debe tener al menos 8 caracteres.');return}if(this.nueva!==this.confirmar){this.error.set('Las contraseñas nuevas no coinciden.');return}this.loading.set(true);try{await this.auth.cambiarPassword(this.actual,this.nueva);this.router.navigateByUrl('/');}catch(e:any){this.error.set(e?.error?.message??'No se pudo cambiar la contraseña.');}finally{this.loading.set(false)}}}